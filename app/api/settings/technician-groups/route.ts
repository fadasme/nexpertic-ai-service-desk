import { requirePermission } from "@/lib/nexera/auth-store";
import { createTechnicianGroup, deleteTechnicianGroup, listTechnicianGroups, updateTechnicianGroup } from "@/lib/nexera/technician-group-store";
import { tenantIdFromRequest } from "@/lib/nexera/tenant-context";
import type { TechnicianGroup } from "@/lib/nexera/contracts";
export async function GET(request: Request) { const authorization = await requirePermission(request, "user:read"); if (!authorization.allowed) return authorization.response; return Response.json({ data: await listTechnicianGroups(await tenantIdFromRequest(request)) }); }
export async function POST(request: Request) { const authorization = await requirePermission(request, "user:update"); if (!authorization.allowed) return authorization.response; const body = (await request.json().catch(() => ({}))) as Partial<TechnicianGroup>; try { const data = await createTechnicianGroup({ name: String(body.name ?? ""), specialty: String(body.specialty ?? "") }, await tenantIdFromRequest(request)); return Response.json({ data }, { status: 201 }); } catch (error) { return Response.json({ error: error instanceof Error ? error.message : "Could not create group" }, { status: 400 }); } }
export async function PATCH(request: Request) { const authorization = await requirePermission(request, "user:update"); if (!authorization.allowed) return authorization.response; const body = (await request.json().catch(() => ({}))) as Partial<TechnicianGroup>; if (typeof body.id !== "string") return Response.json({ error: "id is required" }, { status: 400 }); const data = await updateTechnicianGroup(body.id, { name: typeof body.name === "string" ? body.name.trim() : undefined, specialty: typeof body.specialty === "string" ? body.specialty.trim() : undefined, status: body.status === "Pausado" ? "Pausado" : "Activo" }, await tenantIdFromRequest(request)); if (!data) return Response.json({ error: "Group not found" }, { status: 404 }); return Response.json({ data }); }
export async function DELETE(request: Request) { const authorization = await requirePermission(request, "user:update"); if (!authorization.allowed) return authorization.response; const id = new URL(request.url).searchParams.get("id"); if (!id || !(await deleteTechnicianGroup(id, await tenantIdFromRequest(request)))) return Response.json({ error: "Group not found" }, { status: 404 }); return Response.json({ data: { deleted: true } }); }
